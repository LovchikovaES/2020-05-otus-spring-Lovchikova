spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: data.sql

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: DEBUG